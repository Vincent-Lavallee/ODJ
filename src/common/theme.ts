import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ba1a20",
    },
    text: {
      primary: "#dcdcd4",
    },
    background: {
      default: "#000000",
      paper: "#141313",
    },
  },
});

export default theme;
